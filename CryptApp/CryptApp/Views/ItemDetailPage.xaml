<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CryptApp.Views.ItemDetailPage"
             Title="{Binding Title}">
    <ScrollView >
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Text:" FontSize="Medium" />
            <Label Text="{Binding ItemTitle}" FontSize="Small"/>
            <Label Text="Description:" FontSize="Medium" />
            <Editor Text="{Binding Description}" FontSize="Small"/>
            <Label Text="UserName:" FontSize="Medium" />
            <Editor Text="{Binding UserName}" FontSize="Small"/>
            <Label Text="Email:" FontSize="Medium" />
            <Editor Text="{Binding Email}" FontSize="Small"/>
            <Label Text="Password:" FontSize="Medium" />
            <Editor Text="{Binding Password}" FontSize="Small"/>
            <Label Text="Phone:" FontSize="Medium" />
            <Editor Text="{Binding Phone}" FontSize="Small"/>
            <Label Text="AdditionalInfo:" FontSize="Medium" />
            <Editor Text="{Binding AdditionalInfo}" FontSize="Small"/>
        </StackLayout>
    </ScrollView>
</ContentPage>